name: Customer MCP Release

on:
  push:
    tags:
      - "*.*.*"
    paths:
      - "customer-mcp-container/**"
      - ".github/workflows/customer-mcp-docker-publish.yml"
      - ".github/workflows/customer-mcp-release.yml"

env:
  REGISTRY: ghcr.io
  # github.repository as <account>/<repo>
  IMAGE_NAME: ${{ github.repository }}-customer-mcp

jobs:
  release:
    name: Release Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare environment
        run: |
          repo_name="${{ github.repository }}"
          echo "IMAGE_NAME_LOWERCASE=$(echo "$repo_name" | tr '[:upper:]' '[:lower:]')-customer-mcp" >> $GITHUB_ENV

      - name: Log into registry ${{ env.REGISTRY }}
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Add Release Tag
        run: |
          # Source image linked to the commit sha (built by the publish workflow)
          SRC_IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWERCASE }}:sha-${{ github.sha }}"
          # Target image with the release tag (the git tag name)
          TARGET_IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWERCASE }}:${{ github.ref_name }}"

          echo "Retagging $SRC_IMAGE to $TARGET_IMAGE"
          docker buildx imagetools create -t "$TARGET_IMAGE" "$SRC_IMAGE"
